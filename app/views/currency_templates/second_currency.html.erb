<h1>Convert</h1>

<p>Go <a href="/forex">back</a></p>

<ul>
    <%= @array_of_symbols.each_with_index do |a_symbol, the_index| %>
      <li>
        <a href="/forex/<%= a_symbol %>/<%=@a_symbol.index%>">
        Convert 1 <%= a_symbol %> to <%=@a_symbol.index%>...
        </a>
      </li>
    <% end %>
</ul>
